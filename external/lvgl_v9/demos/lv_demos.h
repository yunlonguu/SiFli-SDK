/**
 * @file lv_demos.h
 *
 * Vendor test adaptation stub.
 *
 * The SDK does not ship LVGL's upstream demos/ content. The PC-hosted unit
 * tests include this header unconditionally; tests that actually call demo
 * functions are guarded by LV_USE_DEMO_* (default 0) and compile to empty.
 * Tests that need demo sources/assets (render demo, demo widgets assets) are
 * excluded in tests/CMakeLists.txt (VENDOR_DEMO_EXCLUDED_TESTS).
 */
#ifndef LV_DEMOS_H
#define LV_DEMOS_H

#ifdef __cplusplus
extern "C" {
#endif

#ifdef __cplusplus
}
#endif

#endif /* LV_DEMOS_H */
